import { SupplierProductCategory } from './supplier-product-category.model';

describe('SupplierProductCategory', () => {
  it('should create an instance', () => {
    expect(new SupplierProductCategory()).toBeTruthy();
  });
});
