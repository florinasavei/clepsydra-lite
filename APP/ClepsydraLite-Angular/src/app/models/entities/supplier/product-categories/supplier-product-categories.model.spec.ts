import { SupplierProductCategories } from './supplier-product-categories.model';

describe('SupplierProductCategories', () => {
  it('should create an instance', () => {
    expect(new SupplierProductCategories()).toBeTruthy();
  });
});
